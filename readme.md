# 📋 Таск-трекер - Модульная архитектура

## 📁 Структура проекта

```
project/
├── index.html              # HTML разметка
├── style.css               # Все стили
├── README.md               # Документация
│
└── js/                     # JavaScript модули
    ├── main.js             # Точка входа приложения
    │
    ├── config/             # Конфигурация
    │   ├── firebase.js     # Firebase настройки и инициализация
    │   ├── telegram.js     # Telegram bot конфигурация
    │   └── constants.js    # Константы приложения
    │
    ├── state/              # Управление состоянием
    │   └── state.js        # Глобальное состояние
    │
    ├── services/           # Сервисы
    │   ├── auth-service.js       # Аутентификация
    │   ├── firebase-service.js   # Работа с Firestore
    │   └── telegram-service.js   # Уведомления в Telegram
    │
    ├── ui/                 # UI компоненты
    │   ├── ui.js           # Основной UI (рендер задач, форм)
    │   ├── modal.js        # Модальные окна
    │   └── datepicker.js   # Календарь выбора даты
    │
    ├── handlers/           # Обработчики событий
    │   └── event-handlers.js  # Все события приложения
    │
    ├── utils/              # Утилиты
    │   ├── utils.js           # Общие утилиты
    │   ├── deadline-timer.js  # Таймер дедлайнов
    │   └── hover-effects.js   # Эффекты наведения
    │
    └── app.js              # Главный класс приложения
```

## Описание модулей

### `config/` - Конфигурация
- **firebase.js** - Firebase config, инициализация app и db
- **telegram.js** - Токен бота и chat ID
- **constants.js** - Все константы (статусы, названия месяцев, etc.)

### `state/` - Состояние
- **state.js** - Централизованное хранилище состояния приложения

### `services/` - Бизнес-логика
- **auth-service.js** - Логин, логаут, проверка авторизации
- **firebase-service.js** - CRUD операции с задачами
- **telegram-service.js** - Отправка уведомлений в Telegram

### `ui/` - Пользовательский интерфейс
- **ui.js** - Рендеринг задач, форм, создание элементов
- **modal.js** - Открытие/закрытие модальных окон, эффекты
- **datepicker.js** - Календарь выбора даты

### `handlers/` - Обработчики
- **event-handlers.js** - Все обработчики событий (клики, submit, scroll)

### `utils/` - Вспомогательные функции
- **utils.js** - Escape HTML, конвертация ссылок, копирование в буфер
- **deadline-timer.js** - Обновление таймеров дедлайнов
- **hover-effects.js** - Интерактивные эффекты при наведении

### Корневые файлы
- **app.js** - Главный класс, инициализация приложения
- **main.js** - Точка входа, запуск приложения

## Как это работает

### 1. Запуск приложения
```javascript
// main.js
document.addEventListener("DOMContentLoaded", () => {
  App.bootstrap();
});
```

### 2. Инициализация
```javascript
// app.js
App.bootstrap() → проверка Auth → init() или renderLoginForm()
```

### 3. Загрузка данных
```javascript
FirebaseService.loadTasks() 
  → dispatch 'tasksLoaded' event
  → UI.renderTasks()
  → DeadlineTimer.start()
```

### 4. Взаимодействие пользователя
```javascript
Клик на задачу 
  → EventHandlers.attachTaskClickHandlers()
  → Modal.open()
  → attachModalHandlers()
```

## 💡 Преимущества модульной структуры

### Читаемость
- Каждый файл отвечает за одну задачу
- Легко найти нужный функционал
- Понятная иерархия

### Масштабируемость
- Легко добавлять новые модули
- Не нужно трогать весь код
- Независимые компоненты

### Тестируемость
- Каждый модуль можно тестировать отдельно
- Явные зависимости через импорты
- Изолированная логика

### Переиспользование
- Модули можно использовать в других проектах
- Сервисы независимы от UI
- Утилиты универсальны

### Поддержка
- Легко найти и исправить баг
- Понятно, что где находится
- Удобно работать в команде

## Потоки данных

### Аутентификация
```
User input → Auth.login() 
  → Auth.setUser() 
  → dispatch 'userLoggedIn' 
  → App.init()
```

### CRUD задач
```
UI interaction → EventHandlers 
  → FirebaseService (create/update/delete)
  → TelegramService.sendNotification()
  → FirebaseService.loadTasks()
  → dispatch 'tasksLoaded'
  → UI.renderTasks()
```

### Модальные окна
```
Click task → Modal.open()
  → UI.createTaskModal()
  → EventHandlers.attachModalHandlers()
  → Modal.attachModalEffects()
```

## Архитектурные паттерны

- **Module Pattern** - каждый файл = модуль с экспортом
- **Observer Pattern** - события для связи между модулями
- **Service Pattern** - бизнес-логика в сервисах
- **Separation of Concerns** - разделение ответственности

## Как добавить новый функционал

### Пример: Добавить фильтрацию по приоритету

1. **Добавить константы** (`config/constants.js`)
```javascript
PRIORITY_LEVELS: ['low', 'medium', 'high']
```

2. **Обновить UI** (`ui/ui.js`)
```javascript
renderPriorityFilter() { ... }
```

3. **Добавить обработчик** (`handlers/event-handlers.js`)
```javascript
attachPriorityFilter() { ... }
```

4. **Обновить сервис** (`services/firebase-service.js`)
```javascript
async loadTasksByPriority(priority) { ... }
```

5. **Подключить в App** (`app.js`)
```javascript
EventHandlers.attachPriorityFilter();
```
